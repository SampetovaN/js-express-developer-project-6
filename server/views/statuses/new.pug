extends ../layouts/application.pug

block header
  = t('views.statuses.new.add')

block content
  +formFor()(action=route('statuses') method='post')
    +input(user, 'name', errors)(type="text")
    +submit(user)(value=t('views.statuses.new.submit'))
